{
  "name": "sls-shipyard-test",
  "version": "0.0.1",
  "description": "Simple Serverless tests for Shipyard",
  "scripts": {
    "deploy": "serverless deploy --stage local",
    "undeploy": "serverless remove --stage local",
    "version": "serverless --version"
  },
  "devDependencies": {
    "serverless": "2.48.0",
    "serverless-shipyard": "^0.4.30"
  }
}
