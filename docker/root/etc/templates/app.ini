APP_NAME = $APP_NAME
RUN_MODE = $RUN_MODE

[repository]
ROOT = /data/git/repositories

[repository.local]
LOCAL_COPY_PATH = /data/shipyard/tmp/local-repo

[repository.upload]
TEMP_PATH = /data/shipyard/uploads

[server]
APP_DATA_PATH = /data/shipyard
DOMAIN           = $DOMAIN
SSH_DOMAIN       = $SSH_DOMAIN
HTTP_PORT        = $HTTP_PORT
ROOT_URL         = $ROOT_URL
DISABLE_SSH      = $DISABLE_SSH
SSH_PORT         = $SSH_PORT
SSH_LISTEN_PORT  = $SSH_LISTEN_PORT
LFS_START_SERVER = $LFS_START_SERVER

[database]
PATH = /data/shipyard/shipyard.db
DB_TYPE = $DB_TYPE
HOST    = $DB_HOST
NAME    = $DB_NAME
USER    = $DB_USER
PASSWD  = $DB_PASSWD
LOG_SQL = false

[indexer]
ISSUE_INDEXER_PATH = /data/shipyard/indexers/issues.bleve

[session]
PROVIDER_CONFIG = /data/shipyard/sessions

[picture]
AVATAR_UPLOAD_PATH = /data/shipyard/avatars
REPOSITORY_AVATAR_UPLOAD_PATH = /data/shipyard/repo-avatars

[attachment]
PATH = /data/shipyard/attachments

[log]
MODE = console
LEVEL = info
ROOT_PATH = /data/shipyard/log

[security]
INSTALL_LOCK = $INSTALL_LOCK
SECRET_KEY   = $SECRET_KEY
REVERSE_PROXY_LIMIT = 1
REVERSE_PROXY_TRUSTED_PROXIES = *

[service]
DISABLE_REGISTRATION = $DISABLE_REGISTRATION
REQUIRE_SIGNIN_VIEW  = $REQUIRE_SIGNIN_VIEW

[lfs]
PATH = /data/git/lfs
